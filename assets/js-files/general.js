/*const largeButtons = document.querySelectorAll('.trans-btn');

largeButtons.forEach(largeButton => {
    largeButton.addEventListener('click', () => {
        // Get the current background color
        const computedStyle = window.getComputedStyle(largeButton);
        const currentBg = computedStyle.backgroundColor;
        
        // Set the background color to black after a delay
        setTimeout(function() {
            largeButton.style.backgroundColor = currentBg;
        }, 150); // Adjust the timing here (in milliseconds)

        // Immediately set the background color to black
        largeButton.style.backgroundColor = 'black';
    });
});*/